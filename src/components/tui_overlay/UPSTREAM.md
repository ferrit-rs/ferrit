# Vendored source

Source copied from [`jharsono/tui-overlay`](https://github.com/jharsono/tui-overlay),
crate version `0.1.2`. Kept as a local module so ferrit can build reusable UI
components on top of the overlay primitives without coupling those components
to the standalone crate boundary.

The upstream dual-license texts are included as `LICENSE-MIT` and
`LICENSE-APACHE`.
